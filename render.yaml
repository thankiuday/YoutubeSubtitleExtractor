services:
  - type: web
    name: youtube-subtitle-extractor
    env: node
    buildCommand: |
      npm install
      cd server
      npm install
      cd ..
      npm run build
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001 